{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only core window capabilities; no generic fs, shell, or process access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

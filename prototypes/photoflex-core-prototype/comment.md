# 改动意见7
- 选择方式变回改动意见6时那样，即单击整张照片勾选，保留预览按钮进入大图，双击不进入大图；
- 增加大图预览时的选择照片与移除照片按钮
- "进入白板"和"进入序列全景"这两个按钮放到标题右侧，删除“直接布置工作区这一模块”；
- 移除Sequence部分拖动模块、拖动说明文字，确保能够一个版面看到保存按钮；






# 改动意見6
- 在contact sheet跟pool的部分都能够能够双击预览大图；
- 重新开始键补上重新开始的功能；
- 在三个阶段的照片选择部分都加上全选/反全选功能；
- 当导入图片太大量太多时，网页会过于卡顿、甚至崩溃，寻找优化办法；
- 修复预览竖图大图时显示不完全的问题，保证每种比例的照片在预览时都能够完整显示；
- 白板部分增加不保留排序退出白板选项；
- 白板增加框选多照片移动、移除的功能；
- 给sequence部分增加一个序列全景模式，进入该模式后页面只呈现照片序列，照片大小为序列模块的1.25倍，在这个模式时点击照片进入大图预览；
- compare部分的沉浸模式也与sequence一样增加序列全景模式；
- compare部分的沉浸预览大图须确保照片能够完整显示；
- 删除照片旋转功能；
- 白板部分削弱照片外框的视觉效果、移除照片名等信息，只保留照片本身与序列号，增加在白板内移除照片功能；
- 移除sequence中的照片时停留在原地，不要回到一开始；






# 改动意见5（UX-05支持本地JPEG）
- 把现有原型改造成支持导入（更省内存的方式应该是读取本地文件夹） 500 张 JPEG 的研究版本；
- pool可导入50张照片；
- 增大白板的大小，使其能容纳50张以上的照片，增加缩放白板视角大小、按住右键拖曳移动白板视角等白板功能（参考miro,milanote等白板功能）；
- compare部分点进一个version的模块进入沉浸预览模式（如改动意见3），保留打开版本返回sequence的按键





# 改动意见4
- Sequence部分，每个模块希望移动的更自由，像是在白板/Powerpoint移动物件一样，调整逻辑是用鼠标拖拽/缩放四角（目前先只保持四方形）；
- 调整模块的部分删除；
- 从pool中选择照片时滚动条留在原地，不回到最上面；
- 添加旋转照片按钮
- 沉浸模式改成进入白板（参考miro的白板），可自由移动位置、调整照片，照片调整大小的方式与白板工具中类似，拖曳四角（或八角），移除底下的调整大小模块，做的排序改动退出白板时要保留在sequence中；



# 改动意见3
目前大致满意，但还有一些改动：
- pool可能会有很多照片，希望模块可自定义大小与位置；
- sequence模块同样可自定义大小与位置，同时每张照片本身可自定义大小，但整体横向布局须维持一定大小（可自由调整，但不受单张照片大小影响）；
- 给sequence部分增加一个沉浸模式，进入该模式后页面只呈现照片序列；
- 点击照片可进入单张照片的大图预览，通过左右键可进行单张照片的序列观看



# 改动意见2（不看）
- 最上方的部分与模块缩小一点
- 每个模块的大标题都缩小一点，大约word字体36-40左右
- sequence部分做两个可选视角，一个是现在的，另一个是只能横向排列的视角，超出框格的部分通过滚动查看，下方增加滚动条，左右增加箭头方便滚动，横向滚动模式为默认；右侧pool模块可以缩小三分之一；
- compare部分，设立保存version机制，可给version整个模块命名并保存，保存好的version做成一个小包放在compare标题右侧，点击可将其变为与星星比较的version



# 改动意见1(已改不用看)
**方案B的UI较好**

以下是改动需求：
- 目前只按方案B来做，将背景变为纯色，最上方的工作阶段按钮做大、附上文字；
- 去掉当前意图，visible prototype state部分；
- 优化操作逻辑，从contact sheet里选择进入pool之后照片变成灰阶，记录已在pool中的照片，不用停在打勾的状态；pool中删除照片后contact sheet里的照片变回原来颜色；
- 把pool跟sequence部分结合，在画面右侧放pool的照片，两列，左边是照片sequence，当勾选右侧照片时左侧sequence出现该照片，在右侧部分反选则取消sequence里的这张照片，sequence的张数随意；
- sequence里的图要能自由拖曳改变顺序
- compare部分过多元素，两个版本上下并排，空间只展现两个version（放大），不需要任何数据；两个version旁边都有memo提供文字记录，；
- 给喜欢的version打星，按另一个version进去回到sequence排序界面排序，比较时永远与打星号的version作比较
- 删除恢复现场部分

自己跑测试，看改动后能不能满足上述要求，全部通过了再提交给我

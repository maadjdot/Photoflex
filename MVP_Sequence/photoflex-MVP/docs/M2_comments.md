# 修改意见02
- Table部分无法通过顶端contact sheet按键回到contact sheet，修复；
- table部分所有照片都应该清晰（接近原片清晰度），选择与否不影响照片清晰度；
- Table部分双击照片进入大图预览；
- table部分不显示照片名称


# 修改意见01
- contact Sheet滚动停下时时会强制回到照片最上方显示完整，移除这个设计，滚动停止就停在原地；
- 大图预览时放缩照片要逐步放大，目前放大照片时会跳转到过度放缩裁剪的画面，可参考其他github项目的办法；
- contact sheet部分，保留像是pool的设计，能够预览在table的照片；
- 增加缩略图的像素（增加20%），牺牲一部分性能；
- 目前Table上的图片要选择后才清晰加载，table上的所有图片的像素都应该是清晰的，放大尺寸；
- 移除Hand模式，将功能以按压右键实现；
- 放缩功能最多达到300%；
- Ctrl+滚轮放缩Table不触发浏览器的放缩功能，只放缩table的部分
- Table部分，目前框选照片的框显示没有跟着光标走，都保持在左上方，修复这个问题使框选跟着光标走；
- table部分，增加双击照片预览大图的功能；
- Table增加选择照片调整照片大小的功能（不改变比例）；
- Table目前无法回到contact sheet界面，修复这个路径

# 修改意见01
- Table中的sequence小方块可以调整大小；
- 目前Versions中选择哪两个version进行比较的UI尚不太清晰，改成选择两个version，然后点compare键，Compare键只需一个，设在versions旁边，黑底白字；
- 增加删除version的功能；
- Version Compare界面变成上下布局，照片增大一倍（界面类似Sequence，只是变成上下两条Sequence），移除照片右下角的Moved/Unchanged字样，只保留纯照片；
- 增加在Version Compare界面中自由选择需要对比的两个序列的功能（可对比在Table中的所有序列），左上角只保留A/B序列与名称，移除Current/Right的说法。